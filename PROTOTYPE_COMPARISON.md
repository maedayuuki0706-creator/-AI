# 4方式の仮想比較

試作バージョン: `four-way-70-30-10points-v1`

| 方式 | 既存の確率 | 日和の確率 | 買い目選択 |
|---|---:|---:|---|
| 既存 | 100% | 0% | 既存の頭・相手選択と折返し補正 |
| 日和 | 0% | 100% | 日和の頭・相手選択 |
| プロトタイプ1（既存優先） | 70% | 30% | 既存の選択方法を使用 |
| プロトタイプ2（日和優先） | 30% | 70% | 日和の選択方法を使用 |

70/30は試作開始時に固定する仮説であり、最適化済みの配分ではない。
両モデルは土台を共有するため、独立した二者の予想とは扱わない。
両試作が同じ買い目になった場合も、そのまま記録する。

## 公平な比較条件

- 同じレースの公式データを一度取得し、実際に稼働している `hiyori_source` / `hiyori_model` で日和を作る。
- 3連単120通りの推定確率を配分し、全方式10点・各100円（1レース1,000円）にそろえる。
- 既存と日和の対照群も、この共通取得時点・10点の条件で再構成する。従来のDiscord配信実績と混ぜない。
- 通常点数の候補は `native_candidate_picks` に保存するが、配信済みとは扱わない。
- 展示6艇、日和の有効な追加情報、締切1分前までの計算完了を必要とする。情報不足を既存のコピーで埋めない。
- 1レースにつき最初に成立した4方式の予想を固定する。締切後に予想を作り直さない。
- オッズは4方式で同じ取得値を使う。欠けたオッズの期待値は不明とする。今回の試作には見送り条件を追加しない。
- 正式結果が取得でき、4方式とも返還以外の有効買い目を持つ共通レースだけで的中率と回収率を比較する。
- 全返還・不成立・特払い等は別に数え、結果待ちや未記録を不的中扱いしない。

## 記録と運用

`data/prototypes/four-way-70-30-10points-v1/YYYYMMDD/` に保存する。

- `predictions/`: 締切前の4方式の買い目・120通りの確率・重み・時刻・コード識別子
- `snapshots/`: 元の公式データ・日和データ・日和分析・折返し補正の学習状態
- `results/`: 公式結果、100円平買いの払戻・返還・トリガミ・万舟判定
- `summary.json`: 共通分母の成績と、各方式だけが的中したレース
- `coverage.json`: 対象レース、記録済み・情報不足・締切超過・開始前終了の区別

GitHub Actionsの既存試作ワークフローを継続監視・再試行・記録保存付きにする。
予想と結果はGit履歴と実行アーティファクトに残す。4方式の試作は仮想記録であり、Discordへの新規投稿や実投票は行わない。
スケジュールの起動遅延はあり得るため、欠けたレースは `coverage.json` に明示する。

結果を見て同じ日の重みを変更しない。配分や点数を変える場合は新しいバージョンにして、別の日に前向きに比較する。

検証: `python -m unittest discover -s tests -p 'test_four_way_prototype.py' -v`

収集: `python prototype_runner.py --watch-seconds 720`
